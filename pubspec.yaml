name: darak_wa_hayk
description: دارك وحيك - منصة عقارية ذكية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_riverpod: ^2.5.1
  cached_network_image: ^3.3.0
  url_launcher: ^6.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  shimmer: ^3.0.0
  flutter_staggered_animations: ^1.1.1
  share_plus: ^7.2.1
  webview_flutter: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
